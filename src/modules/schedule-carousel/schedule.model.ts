export interface Schedule {
    mediaId: number;
    description: string;
    start: string;
    duration: string;
    episodeTitle: string;
    programTitle: string;
    thumbnail: string;
    hasVideo: boolean;
}