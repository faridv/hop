import styled from 'styled-components';

export const SepehrRowWrapper = styled.div`
    margin-bottom: .5rem;
    padding: 1rem;
`;

export const SepehrRowTitle = styled.div`
    color: white;
    margin-bottom: 1rem;
    font-weight: bold;
`;

export const SepehrRowScrollWrapper = styled.div`
    overflow-x: auto;
    overflow-y: hidden;
    flex-shrink: 1;
    flex-grow: 1;
    padding-left: 60px;
`;

export const SepehrRowScrollingContent = styled.div`
    display: flex;
    flex-direction: row;
`;
